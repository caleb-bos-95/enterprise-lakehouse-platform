# Architecture

Architecture diagrams and Architecture Decision Records (ADRs).

## Structure

- `diagrams/` — Architecture and data flow diagrams
- ADRs are numbered sequentially: `ADR-001-iceberg-as-storage-format.md`

## Architecture Decisions

Key decisions recorded here include storage format selection (Iceberg), query engine selection (Trino), streaming platform (Redpanda), and orchestrator (Airflow).