# =============================================================================
# Trino — TPC-H Connector (built-in benchmark / test catalog)
# =============================================================================
# Useful for verifying Trino is healthy without needing Iceberg tables.
#   SELECT * FROM tpch.tiny.orders LIMIT 10;
# =============================================================================

connector.name=tpch
