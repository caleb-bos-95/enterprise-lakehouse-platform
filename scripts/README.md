# Scripts

Operational utilities and setup scripts.

## Planned Scripts

- `setup_minio_buckets.sh` — Manual bucket initialisation (fallback to minio-init container)
- `create_redpanda_topics.sh` — Create required Kafka topics
- `seed_openmetadata.py` — Bootstrap OpenMetadata with dataset registrations and glossary